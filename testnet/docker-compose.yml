version: "3.4"

services:
  xinfinnetwork:
    image: xinfinorg/xdposchain:v1.4.8-beta
    volumes:
      - "./xdcchain:/work/xdcchain"
    restart: "always"
    env_file: .env
    network_mode: "host"
